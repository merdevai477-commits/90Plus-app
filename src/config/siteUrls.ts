import { SHARE_BASE_URL } from './shareLinks';

/** Public site origin — never use Railway URL in user-facing content */
export const PUBLIC_SITE_URL = SHARE_BASE_URL;

/** HTTP Referer / OpenRouter site attribution */
export const PUBLIC_SITE_ORIGIN = PUBLIC_SITE_URL;
