# استراتيجية التحديثات - 90Plus App 🚀

## 🎯 **متى تحتاج Build جديد؟**

### ❌ **Build جديد مطلوب:**
- إضافة مكتبات native جديدة
- تغيير permissions في app.json
- تحديث Expo SDK
- تغيير App Icon أو Splash Screen
- إضافة native modules (Camera, Location, etc.)
- تغيير Bundle ID أو App Name

### ✅ **بدون Build - مجرد Push:**

#### **تحديثات الباك إند:**
```bash
git add . && git commit -m "Backend updates" && git push
```
- إضافة أسئلة كويز جديدة
- تحديث بيانات المباريات
- إصلاح API bugs
- إضافة endpoints جديدة
- تحديث صفحات الدعم

#### **تحديثات الفرونت إند - OTA:**
```bash
npx expo publish
```
- تحسين UI/UX
- إصلاح JavaScript bugs
- تحديث النصوص والترجمات
- تحسين الأداء
- إضافة مميزات JavaScript فقط

## 📅 **استراتيجية التحديث المقترحة:**

### **🔄 تحديثات مستمرة (يومية/أسبوعية):**

#### **الباك إند:**
- إضافة أسئلة كويز جديدة
- تحديث بيانات الدوريات
- إصلاح bugs
- تحسين الأداء

#### **الفرونت إند:**
- تحسينات UI
- إصلاح bugs بسيطة
- تحديث المحتوى

### **📱 Builds جديدة (شهرية أو عند الحاجة):**

#### **متى تعمل Build جديد:**
1. **تجميع التحديثات الشهرية**
2. **إضافة مميزات كبيرة**
3. **تحديث مكتبات مهمة**
4. **إصلاح مشاكل كبيرة**

#### **الجدولة المقترحة:**
- **الأسبوع 1-3:** تحديثات OTA + Backend
- **الأسبوع 4:** Build جديد للـ App Store

## 🎯 **أمثلة عملية:**

### **✅ بدون Build جديد:**

#### **إضافة أسئلة كويز:**
```typescript
// Backend/src/data/quiz-questions/legends-complete.ts
{
  id: "legends-022",
  question: "من فاز بكأس أمم أفريقيا 2024؟",
  options: ["ساحل العاج", "نيجيريا", "السنغال", "المغرب"],
  correctAnswer: "0",
  // ...
}
```
**النتيجة:** الأسئلة تظهر فوراً في التطبيق!

#### **تحسين صفحة المباريات:**
```bash
# تعديل front/app/(tabs)/matches.tsx
npx expo publish
```
**النتيجة:** المستخدمين يحصلوا على التحديث خلال دقائق!

### **❌ يحتاج Build جديد:**

#### **إضافة كاميرا للريلز:**
```json
// app.json
"plugins": [
  "expo-camera"  // ← مكتبة native جديدة
]
```
**النتيجة:** محتاج Build جديد للـ App Store

#### **تغيير الأيقونة:**
```json
// app.json
"icon": "./assets/images/new-icon.png"
```
**النتيجة:** محتاج Build جديد

## 🚀 **الفوائد:**

### **تحديثات سريعة:**
- ✅ **المستخدمين يحصلوا على المميزات فوراً**
- ✅ **مفيش انتظار لـ App Store Review**
- ✅ **إصلاح Bugs بسرعة**

### **تحديثات مجمعة:**
- ✅ **تجربة مستخدم أفضل**
- ✅ **تقليل عدد التحديثات المطلوبة**
- ✅ **استقرار أكثر**

## 📊 **مثال لشهر كامل:**

### **الأسبوع الأول:**
```bash
# إضافة 20 سؤال جديد
git push  # ← فوري

# تحسين UI الكويز
npx expo publish  # ← خلال دقائق
```

### **الأسبوع الثاني:**
```bash
# إصلاح bug في التوقعات
npx expo publish

# تحديث صفحة الدعم
git push
```

### **الأسبوع الثالث:**
```bash
# إضافة ميزة الإشعارات المحسنة
npx expo publish

# تحسين أداء الباك إند
git push
```

### **الأسبوع الرابع:**
```bash
# Build شامل للـ App Store
eas build --platform ios --profile production
eas submit --platform ios --profile production
```

## 🎯 **التوصيات:**

### **للتطوير اليومي:**
1. **استخدم OTA للفرونت إند**
2. **استخدم Git Push للباك إند**
3. **اختبر التحديثات قبل النشر**

### **للإصدارات الكبيرة:**
1. **اجمع التحديثات لمدة شهر**
2. **اعمل Build شامل**
3. **ارفع على TestFlight للاختبار**
4. **انشر على App Store**

## ✅ **الخلاصة:**

**90% من التحديثات مش محتاجة Build جديد!**

- **أسئلة كويز جديدة** → Git Push
- **تحسينات UI** → Expo Publish  
- **إصلاح Bugs** → Expo Publish
- **مميزات JavaScript** → Expo Publish

**فقط 10% محتاجة Build جديد:**
- **مكتبات Native جديدة**
- **تغيير App Settings**
- **تحديثات كبيرة شهرية**

---

**النتيجة: تطوير أسرع وتحديثات أكثر! 🚀**