/**
 * TEMP FREEZE — hide fixtures on Predictions tab until product re-enables.
 * Must stay in sync with backend temp-surface-freeze for groups/AI.
 */
export const TEMP_FREEZE_PREDICTIONS_TAB_MATCHES = true;
