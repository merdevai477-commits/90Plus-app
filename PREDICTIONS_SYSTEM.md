# 🎯 نظام التوقعات - Predictions System

## نظرة عامة

تم إضافة نظام توقعات كامل في صفحة المباريات يسمح للمستخدمين بالتوقع على نتائج المباريات القادمة.

---

## المميزات الرئيسية

### ✅ 1. تاب التوقعات
- تم إضافة تاب جديد "التوقعات" 🎯 في صفحة المباريات
- يعرض فقط المباريات القادمة (التي لم تبدأ بعد)
- واجهة مستخدم سهلة وجذابة

### ✅ 2. أنواع التوقعات
المستخدم يمكنه التوقع بـ:
- **فوز الفريق المضيف** (Home Win) 🏠
- **التعادل** (Draw) ⚖️
- **فوز الفريق الضيف** (Away Win) ✈️

### ✅ 3. نظام التذاكر (Coins)
- **تكلفة كل توقع:** 5 تذاكر زرقاء 💎
- **مكافأة التوقع الصحيح:** +10 تذاكر 🎁
- يتم خصم التذاكر فوراً عند التوقع
- يتم إضافة المكافأة عند ظهور النتيجة الصحيحة

### ✅ 4. الحدود اليومية
- **الحد الأقصى للتوقعات:** 10 توقعات يومياً
- يتم إعادة تعيين العداد كل 24 ساعة
- عرض التوقعات المتبقية في أعلى الصفحة

### ✅ 5. عرض النتائج
- بعد انتهاء المباراة، يتم عرض:
  - ✅ **توقع صحيح:** بانر أخضر مع المكافأة
  - ❌ **توقع خاطئ:** بانر أحمر
- تاريخ كامل لجميع التوقعات السابقة

---

## البنية التقنية

### Frontend Components

#### 1. **MatchTabs.tsx**
- تم إضافة تاب `predictions` الجديد
- التاب يظهر أيقونة 🎯

#### 2. **PredictionsSection.tsx** (جديد)
المكون الرئيسي للتوقعات، يحتوي على:
- عرض المباريات القادمة
- أزرار التوقع (فوز/تعادل/خسارة)
- التكامل مع نظام الكوينات
- عرض النتائج والمكافآت
- التحقق من الحدود اليومية

#### 3. **predictions.service.ts** (جديد)
خدمة التواصل مع API، تحتوي على:
```typescript
- getRemainingPredictions() // الحصول على التوقعات المتبقية
- submitPrediction() // إرسال توقع جديد
- getUserPredictions() // الحصول على توقعات المستخدم
- getMatchPredictionCount() // عدد التوقعات على مباراة محددة
```

#### 4. **matches.tsx**
- إضافة فلتر للمباريات القادمة في حالة `predictions`
- إضافة render للـ PredictionsSection
- إخفاء QuickIndicators في تاب التوقعات

### Backend API

#### Endpoints المتاحة:

1. **GET /api/predictions/remaining**
   - الحصول على عدد التوقعات المتبقية لليوم
   - Response: `{ remaining, total, used, coins, predictionCost }`

2. **POST /api/predictions**
   - إرسال توقع جديد
   - Body: `{ apiMatchId, predictionType, homeTeam, awayTeam, ... }`
   - يتحقق من:
     - وجود تذاكر كافية
     - عدم تكرار التوقع
     - الحد اليومي

3. **GET /api/predictions/user**
   - الحصول على جميع توقعات المستخدم
   - Response: `{ predictions[], predictionsMap }`

4. **GET /api/predictions/match/:matchId/count**
   - عدد التوقعات على مباراة محددة
   - Response: `{ home, draw, away, total }`

---

## التكامل مع أنظمة أخرى

### 🪙 نظام الكوينات (CoinsContext)
- استخدام `subtractCoins()` عند التوقع
- استخدام `addCoins()` عند التوقع الصحيح
- التحديث الفوري في الواجهة

### 🌐 نظام الترجمة (i18n)
تمت إضافة الترجمات في جميع اللغات:
- العربية: "التوقعات"
- English: "Predictions"
- Français: "Prédictions"
- Español: "Predicciones"
- Deutsch: "Vorhersagen"
- Italiano: "Previsioni"
- Português: "Previsões"
- Türkçe: "Tahminler"

---

## سيناريوهات الاستخدام

### السيناريو 1: المستخدم يقوم بتوقع جديد
1. المستخدم يفتح تاب "التوقعات"
2. يرى المباريات القادمة
3. يختار توقعه (فوز/تعادل/خسارة)
4. يتم خصم 5 تذاكر
5. يظهر تأكيد بنجاح التوقع
6. الزر يصبح غير نشط (لا يمكن تغيير التوقع)

### السيناريو 2: المستخدم ليس لديه تذاكر كافية
1. المستخدم يحاول التوقع
2. يظهر تنبيه: "تذاكر غير كافية"
3. يعرض الرصيد الحالي والمطلوب

### السيناريو 3: وصول الحد الأقصى اليومي
1. المستخدم قام بـ 10 توقعات
2. يحاول التوقع مرة أخرى
3. يظهر تنبيه: "حد التوقعات اليومي"
4. يُطلب منه الانتظار حتى الغد

### السيناريو 4: عرض نتائج التوقعات
1. بعد انتهاء المباراة
2. النظام يقارن التوقع بالنتيجة الفعلية
3. إذا كان صحيحاً:
   - ✅ بانر أخضر "توقع صحيح! +10 تذاكر"
   - إضافة 10 تذاكر تلقائياً
4. إذا كان خاطئاً:
   - ❌ بانر أحمر "توقع خاطئ"

---

## التحسينات المستقبلية المحتملة

### 🚀 مقترحات للتطوير
1. **إحصائيات شخصية:**
   - نسبة التوقعات الصحيحة
   - عدد التوقعات الإجمالية
   - أفضل دوري للتوقعات

2. **ترتيب المتوقعين:**
   - Leaderboard للمستخدمين
   - مكافآت للأفضل أسبوعياً/شهرياً

3. **توقعات متقدمة:**
   - توقع النتيجة الدقيقة (1-0, 2-1, etc.)
   - توقع الهداف الأول
   - مكافآت أعلى للتوقعات الصعبة

4. **إشعارات:**
   - تذكير قبل بدء المباراة
   - إشعار عند ظهور نتيجة التوقع

5. **تحليلات مساعدة:**
   - إحصائيات الفريقين
   - نسب التوقعات من المستخدمين الآخرين
   - آخر 5 مباريات

---

## ملاحظات هامة للمطورين

### ⚠️ تذكيرات
1. **التوقعات غير قابلة للتعديل:** بمجرد إرسال التوقع، لا يمكن تغييره
2. **المباريات القادمة فقط:** التوقع متاح فقط للمباريات التي لم تبدأ
3. **التحديث التلقائي:** نتائج التوقعات يتم تحديثها تلقائياً عند تحديث نتائج المباريات
4. **الحدود اليومية:** تُحسب بناءً على التوقيت المحلي للمستخدم

### 🔧 Configuration
الثوابت القابلة للتعديل في `Backend/src/routes/predictions.routes.ts`:
```typescript
const DAILY_PREDICTION_LIMIT = 10; // الحد الأقصى اليومي
const PREDICTION_COST = 5; // تكلفة التوقع
const CORRECT_PREDICTION_REWARD = 10; // مكافأة التوقع الصحيح
```

---

## الخلاصة

تم تطوير نظام توقعات متكامل وفعال يتميز بـ:
- ✅ واجهة مستخدم سهلة وجذابة
- ✅ تكامل كامل مع نظام التذاكر
- ✅ حدود عادلة ومكافآت محفزة
- ✅ دعم كامل للترجمات
- ✅ أداء ممتاز وتجربة مستخدم سلسة

**الحالة:** ✅ جاهز للاستخدام

**آخر تحديث:** يناير 2026
